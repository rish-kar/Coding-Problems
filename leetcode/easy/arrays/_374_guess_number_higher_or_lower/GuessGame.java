package easy.arrays._374_guess_number_higher_or_lower;

/**
 * LeetCode provides this base class at runtime.
 * This stub exists only so the solution compiles locally.
 */
public class GuessGame {

    // In LeetCode this is implemented by the judge.
    // Here we return 0 by default to keep compilation working.
    protected int guess(int num) {
        return 0;
    }
}

