package easy.arrays._374_guess_number_higher_or_lower;

/**
 * LeetCode problem 374 provides the judge API `guess(int)`.
 * This stub exists so the local test harness compiles.
 */
public abstract class GuessNumberHigherOrLower {

    /**
     * @return -1 if num is higher than pick, 1 if num is lower than pick, otherwise 0
     */
    protected abstract int guess(int num);

    public int guessNumber(int n) {


        return -1;
    }
}
