public class RotateListTest {
    public static void main(String[] args) {
        RotateList solver = new RotateList();

        System.out.println("RotateListTest scaffold ready. Implement RotateList.rotateRight and then replace these notes with real assertions.");

        // Suggested test cases:
        // 1) head = [1,2,3,4,5], k = 2 -> [4,5,1,2,3]
        // 2) head = [0,1,2], k = 4 -> [2,0,1]
        // 3) head = [], k = 0 -> []
    }
}
