public class WildcardMatchingTest {
    public static void main(String[] args) {
        WildcardMatching solver = new WildcardMatching();

        System.out.println("WildcardMatchingTest scaffold ready. Implement WildcardMatching.isMatch and then replace these notes with real assertions.");

        // Suggested test cases:
        // 1) s = "aa", p = "a" -> false
        // 2) s = "aa", p = "*" -> true
        // 3) s = "cb", p = "?a" -> false
    }
}
